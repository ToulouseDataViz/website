import { graphql } from 'gatsby';

import PageBanner from '../../components/PageBanner';
import Button from '../../components/Button';
import HackavizFooter from '../../components/HackavizFooter';

<PageBanner styleId={1} title={'Hackaviz 2023'} description={'Les données'} />

<section id="one">
<div className="inner">

<Button link={'https://github.com/ToulouseDataViz/Hackaviz-2023'} text={'Voir la page Github dédiée aux données'} />

<!--
<div
  className="blog-post-content"
  dangerouslySetInnerHTML={{ __html: props.data.allFile.nodes[0].childMarkdownRemark.html }}
/>
-->

<HackavizFooter year={'2023'} />

</div>
</section>

<!--
export const pageQuery = graphql`
  query {
    allFile(filter: { name: { eq: "hackaviz23_description" } }) {
      nodes {
        childMarkdownRemark {
          html
        }
      }
    }
  }
`;
-->
